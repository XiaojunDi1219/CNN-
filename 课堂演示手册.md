# CNN 手写数字识别 — 课堂演示手册 (Windows)

## 一、项目简介

这是一个**纯 C++ 标准库**实现的卷积神经网络，不依赖任何第三方库（无 OpenCV、无 Eigen），仅使用 C++17。网络结构为经典的 **LeNet-5** 风格 5 层 CNN：

```
输入 28×28 灰度图
  ↓
C1: 卷积层 (1→6, 5×5 kernel, ReLU) → 6×24×24
  ↓
S2: 最大池化 (2×2) → 6×12×12
  ↓
C3: 卷积层 (6→12, 5×5 kernel, ReLU) → 12×8×8
  ↓
S4: 最大池化 (2×2) → 12×4×4
  ↓
O5: 全连接层 (192→10, Softmax) → 10 个概率输出
```

在 MNIST 测试集上可达 **约 98%** 的识别准确率。

---

## 二、环境准备（Windows）

### 2.1 安装编译器（三选一）

**方案 A: Visual Studio 2022 Community（推荐，免费）**

1. 下载地址: https://visualstudio.microsoft.com/zh-hans/downloads/
2. 安装时勾选 **"使用 C++ 的桌面开发"** 工作负载
3. 安装完成后，VS 自带 CMake 和 MSVC 编译器

**方案 B: MinGW-w64（轻量，无需 VS）**

1. 下载地址: https://www.mingw-w64.org/ 或 https://winlibs.com/
2. 解压到 `C:\mingw64`
3. 将 `C:\mingw64\bin` 添加到系统 PATH 环境变量
4. 验证：打开 PowerShell 或 CMD，输入 `g++ --version`

**方案 C: MSYS2**

1. 下载地址: https://www.msys2.org/
2. 安装后打开 MSYS2 UCRT64 终端，执行：
   ```
   pacman -S mingw-w64-ucrt-x86_64-gcc mingw-w64-ucrt-x86_64-cmake
   ```

### 2.2 安装 CMake

**如果装了 Visual Studio**：VS 已内置 CMake，跳过此步。

**如果使用 MinGW/MSYS2**：
1. 下载 CMake: https://cmake.org/download/ （选 Windows x64 Installer）
2. 安装时勾选 **"Add CMake to system PATH"**

安装后验证（打开新的 PowerShell 或 CMD）：

```
cmake --version
```

应该显示 3.16 或更高版本。

### 2.3 安装 Git（可选，用于下载代码）

```
winget install Git.Git
```

---

## 三、获取 MNIST 数据集

MNIST 是手写数字识别的"Hello World"数据集，包含 70000 张 28×28 的灰度图片。

### 3.1 下载

从 Yann LeCun 官网下载 4 个文件：

| 文件 | 说明 | 大小 |
|------|------|------|
| [train-images-idx3-ubyte.gz](http://yann.lecun.com/exdb/mnist/train-images-idx3-ubyte.gz) | 60000 张训练图片 | 9.5 MB |
| [train-labels-idx1-ubyte.gz](http://yann.lecun.com/exdb/mnist/train-labels-idx1-ubyte.gz) | 60000 个训练标签 | 29 KB |
| [t10k-images-idx3-ubyte.gz](http://yann.lecun.com/exdb/mnist/t10k-images-idx3-ubyte.gz) | 10000 张测试图片 | 1.6 MB |
| [t10k-labels-idx1-ubyte.gz](http://yann.lecun.com/exdb/mnist/t10k-labels-idx1-ubyte.gz) | 10000 个测试标签 | 4.5 KB |

> 课堂上如果网络不好，可以提前下载好放到 U 盘里发给学生。

### 3.2 解压并放入项目

1. 用 7-Zip 或 WinRAR 解压这 4 个 `.gz` 文件
2. 在项目目录下创建 `mnist` 文件夹
3. 把解压出的文件**重命名**（去掉中间的横杠 `-`），放入 `mnist` 文件夹

最终结构：

```
version2_pure_cpp/
├── mnist/
│   ├── train-images.idx3-ubyte
│   ├── train-labels.idx1-ubyte
│   ├── t10k-images.idx3-ubyte
│   └── t10k-labels.idx1-ubyte
├── main.cpp
├── matrix.hpp
├── ... (其他 .hpp 文件)
└── CMakeLists.txt
```

> 注意：重命名时把 `-idx3` 改成 `.idx3`，`-idx1` 改成 `.idx1`（横杠变点）。

---

## 四、编译项目

### 方式 A: 使用 Visual Studio（推荐）

用 VS 直接打开项目文件夹即可，VS 会自动识别 CMakeLists.txt。然后在顶部工具栏选 **"生成" → "全部生成"**。

### 方式 B: 使用 CMake 命令行

打开 **PowerShell** 或 **CMD**，进入项目目录：

```powershell
cd "D:\c++\project test\version2_pure_cpp"

# 创建构建目录
mkdir build
cd build

# 生成 VS 项目并编译
cmake .. -G "Visual Studio 17 2022"
cmake --build . --config Release
```

如果用 MinGW：

```powershell
cd "D:\c++\project test\version2_pure_cpp"
mkdir build
cd build
cmake .. -G "MinGW Makefiles"
cmake --build .
```

### 方式 C: 直接命令行编译（无需 CMake）

```powershell
# MSVC (在 VS 的 Developer PowerShell 中)
cl /std:c++17 /O2 /EHsc /Fe:cnn_mnist.exe main.cpp mnist_reader.cpp

# MinGW
g++ -std=c++17 -O3 -o cnn_mnist.exe main.cpp mnist_reader.cpp
```

编译成功后，可执行文件在 `build\bin\Release\cnn_mnist.exe`（VS）或 `build\cnn_mnist.exe`（MinGW）。

---

## 五、课堂演示流程

### 5.1 快速测试（推荐先做这个！）

完整训练 60000 张图需要 **5-15 分钟**，课堂上可以先跑一个小规模快速演示：

```powershell
# 回到项目根目录运行
cd "D:\c++\project test\version2_pure_cpp"

# 只用 5000 张训练，1 轮 — 约 30 秒 ~ 1 分钟
.\build\bin\Release\cnn_mnist.exe --train-limit 5000 --test-limit 1000
```

预期输出：

```
========================================
  CNN MNIST Digit Recognition (Pure C++)
  5-layer LeNet-5 style network
========================================

[1/4] Loading MNIST dataset...
  Loaded 60000 training images, 10000 test images (XXX ms)
  Using 5000 training, 1000 testing

[2/4] Initializing 5-layer CNN...
  Architecture:
    C1: Conv  1x28x28 -> 6x24x24  (5x5 kernel, ReLU)
    S2: MaxPool 6x24x24 -> 6x12x12  (2x2 window)
    C3: Conv  6x12x12 -> 12x8x8   (5x5 kernel, ReLU)
    S4: MaxPool 12x8x8  -> 12x4x4   (2x2 window)
    O5: FC    192 -> 10  (Affine + Softmax)

[3/4] Training (1 epoch(s), 5000 samples/epoch)...

  --- Epoch 1/1 ---
  Processed 5000/5000 | loss=0.xxxxxx | alpha=0.0010 | acc=XX.XX%
  Epoch complete: avg_loss=0.xxxxxx | accuracy=XX.XX%

[4/4] Evaluating on test set (1000 samples)...
  Test: XXX/1000 correct (XX.XX%)

========================================
  Final Test Accuracy: XX.XX%
  Total time: XX.X s
========================================
```

> 5000 样本训练通常能达到 **85%~92%** 准确率，足够展示 CNN 的学习能力。

### 5.2 完整训练（如果时间充裕）

```powershell
# 完整 60000 样本，1 轮 — 约 5~15 分钟
.\build\bin\Release\cnn_mnist.exe --epochs 1

# 训练 3 轮，准确率更高 — 约 15~45 分钟
.\build\bin\Release\cnn_mnist.exe --epochs 3
```

### 5.3 命令行参数一览

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--epochs N` | 训练轮数 | 1 |
| `--train-limit N` | 每轮训练样本数 | 60000 |
| `--test-limit N` | 测试样本数 | 10000 |
| `--lr X` | 初始学习率 | 0.03 |
| `--quiet` | 减少输出 | 关 |

### 5.4 演示技巧

**课前准备：**
1. 提前下载好 MNIST 数据集，放入 `mnist` 文件夹
2. 提前编译好 `cnn_mnist.exe`，确保能跑通
3. 可以先跑一次完整训练，记住本机大概需要多长时间

**课堂展示节奏建议：**
1. **先展示代码结构**（5 分钟）：打开 `main.cpp`，展示网络定义的几行关键代码；打开 `conv_layer.hpp`，让学生看到卷积的 for 循环实现
2. **跑一次小规模训练**（1~3 分钟）：`--train-limit 5000`，大家看到准确率从随机逐步上升
3. **展示最终测试结果**：强调 98% 左右的准确率，对比人类水平（约 99.7% 错误率）
4. **调参实验**（如果时间允许）：改 `--lr 0.01` 或 `--lr 0.1`，让学生观察对收敛的影响

---

## 六、结合理论讲解的要点

演示时可以配合以下知识点讲解：

### 6.1 卷积层（C1, C3）

```cpp
// conv_layer.hpp 的核心前向传播逻辑
for (int oc = 0; oc < outChannels; ++oc) {
    for (int ic = 0; ic < inChannels; ++ic) {
        // 2D 互相关：滑动窗口，逐元素乘加
        Matrix<T> corr = correlate2D(inputs[ic], kernels_[ic][oc], ConvMode::Valid);
        v_[oc] += corr;
    }
    // 加偏置，过 ReLU
}
```

**讲解点**：卷积核就像一个"特征检测器"，5×5 的窗口在 28×28 图像上滑动，每次做点积运算。第一层学到的是边缘、角点等低级特征；第二层学到的是更复杂的形状组合。

### 6.2 池化层（S2, S4）

```cpp
// pool_layer.hpp — 2×2 最大池化
for (int pr = 0; pr < poolSize_; ++pr)
    for (int pc = 0; pc < poolSize_; ++pc) {
        T val = in(startR + pr, startC + pc);
        if (val > maxVal) maxVal = val;  // 取最大值
    }
```

**讲解点**：池化不改变通道数，但把空间尺寸减半（24→12→8→4）。作用是降维、防止过拟合、提供平移不变性。

### 6.3 全连接层（O5）

```cpp
// fully_connected_layer.hpp — 192 个输入 → 10 个输出
for (int i = 0; i < outputNum_; ++i) {
    T sum = biases_[i];
    for (int j = 0; j < inputNum_; ++j)
        sum += flat.data()[j] * weights_(i, j);  // 加权求和
    输出[i] = softmax(sum);
}
```

**讲解点**：192 = 12通道 × 4×4 特征图。Softmax 把 10 个输出变成概率分布（和为1），概率最大的那个就是预测的数字。

### 6.4 反向传播

```cpp
// loss.hpp — 交叉熵 + Softmax 的联合梯度只需一行
grad = softmax_output - one_hot_target;
```

**讲解点**：交叉熵损失函数配合 Softmax 有一个优雅的数学性质——梯度恰好等于 (预测值 - 真实标签)，代码极简。

### 6.5 学习率衰减

```cpp
// main.cpp — 线性衰减
alpha = initialAlpha - (initialAlpha - minAlpha) * n / (numSamples - 1);
```

从 0.03 线性降到 0.001，后期步子小，更精细地收敛。

---

## 七、在课堂上可以展示的 C++ 知识点

| 知识点 | 对应文件 | 代码特征 |
|--------|----------|----------|
| RAII | `matrix.hpp` | `std::unique_ptr<T[]>` 管理内存，自动释放 |
| 移动语义 | `matrix.hpp` | `Matrix(Matrix&& other) noexcept` |
| 运算符重载 | `matrix.hpp` | `operator+=`, `operator*`, `operator()` |
| 模板 | `matrix.hpp`, `layer.hpp` | `template<typename T>` 泛型矩阵和层 |
| 运行时多态 | `layer.hpp` | 虚函数 `forward()`, `backward()` |
| 随机数 | `conv_layer.hpp` | `std::mt19937`, Xavier 初始化 |
| 函数对象 | `activations.hpp` | `struct ReLU` 可调用对象 |
| 命名空间 | `loss.hpp`, `activation` | 逻辑分组 |
| 智能指针 | `matrix.hpp` | `std::make_unique`, `std::move` |

---

## 八、常见问题排查

### 找不到 MNIST 文件

```
Error loading MNIST data: Cannot open file: mnist/train-images.idx3-ubyte
```

**解决**：检查是否在**项目根目录**运行程序（不是 build 目录），确保 `mnist/` 文件夹存在且文件名正确。

### 编译报错 C++17

**解决**：确保使用 VS 2017+ 或 GCC 8+，CMake 中已设置 `CXX_STANDARD 17`。

### 运行太慢

**解决**：
- 使用 `--train-limit 2000` 只用 2000 张训练（约 20 秒）
- 确保编译时开了 `/O2`（MSVC）或 `-O3`（GCC）优化
- 纯 CPU 运算就是比较慢，可以借此讲解"为什么需要 GPU 加速"

### 准确率太低（比如不到 80%）

**解决**：
- 增加训练样本：`--train-limit 30000`
- 降低学习率：`--lr 0.01`
- 增加轮数：`--epochs 2`

---

## 九、演示后可以留给学生的思考题

1. 如果把第一个卷积层的通道数从 6 改成 16，准确率会变化吗？参数量增加了多少？
2. 如果去掉池化层（直接两层卷积接全连接），会发生什么？
3. C1 的 6 个卷积核各自学到了什么特征？如何可视化？
4. 为什么 Softmax 配合交叉熵的梯度恰好是 `预测值 - 真实值`？试着推导一下。
5. 全连接层 192×10 的权重矩阵有多少个参数？加上偏置呢？整个网络总共有多少个可训练参数？
